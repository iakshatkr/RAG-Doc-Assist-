# RAG-Doc-Assist-
I built a RAG-based document assistant where users can query a set of documents. I implemented document ingestion, chunking, embedding generation, and stored vectors in FAISS. When a query is asked, the system retrieves the top-3 relevant chunks and passes them to an LLM to generate a contextual answer.
